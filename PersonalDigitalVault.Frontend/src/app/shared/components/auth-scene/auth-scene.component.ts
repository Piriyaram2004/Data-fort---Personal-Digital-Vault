import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';
@Component({
  selector: 'app-auth-scene',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './auth-scene.component.html',
})
export class AuthSceneComponent {}
